<%= form_with(model: screen, local: true) do |form| %>
  <% if screen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(screen.errors.count, "error") %> prohibited this screen from being saved:</h2>

      <ul>
      <% screen.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :screen_no %>
    <%= form.number_field :screen_no %>
  </div>

  <div class="field">
    <%= form.label :film_playing %>
    <%= form.text_field :film_playing %>
  </div>

  <div class="field">
    <%= form.label :cinema_id %>
    <%= form.number_field :cinema_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
